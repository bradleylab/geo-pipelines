name: build ortho-pipeline

# Builds and pushes ghcr.io/bradleylab/ortho-pipeline (thin layer over
# opendronemap/odm; the ODM base is large).
#   - PRs touching ortho-pipeline/**  -> build + smoke only (no push)
#   - push to main                    -> :latest
#   - tag ortho-pipeline-v*           -> :vN and :latest

on:
  push:
    branches: [main]
    paths:
      - 'ortho-pipeline/**'
      - '.github/workflows/build-ortho-pipeline.yml'
    tags:
      - 'ortho-pipeline-v*'
  pull_request:
    paths:
      - 'ortho-pipeline/**'
      - '.github/workflows/build-ortho-pipeline.yml'
  workflow_dispatch:
    inputs:
      version_tag:
        description: 'Stable version tag (e.g. v1) — empty = latest only'
        required: false
        default: ''

env:
  IMAGE_NAME: bradleylab/ortho-pipeline
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Free up runner disk (ODM base image is large)
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android /opt/hostedtoolcache/CodeQL || true
          df -h /
      - uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Compute tags
        id: tags
        run: |
          TAGS="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          if [[ "${GITHUB_REF}" == refs/tags/ortho-pipeline-v* ]]; then
            VTAG="${GITHUB_REF#refs/tags/ortho-pipeline-}"
            TAGS="${TAGS},${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VTAG}"
          fi
          if [[ -n "${{ github.event.inputs.version_tag }}" ]]; then
            TAGS="${TAGS},${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.event.inputs.version_tag }}"
          fi
          echo "tags=${TAGS}" >> "${GITHUB_OUTPUT}"
          echo "computed tags: ${TAGS}"
      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: ortho-pipeline
          file: ortho-pipeline/Dockerfile
          push: false
          load: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max
      - name: Smoke test
        run: |
          IMAGE="$(cut -d, -f1 <<<"${{ steps.tags.outputs.tags }}")"
          docker run --rm "${IMAGE}" --help
      - name: Push image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: ortho-pipeline
          file: ortho-pipeline/Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
